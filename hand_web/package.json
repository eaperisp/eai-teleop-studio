{
  "name": "dexterous-hand-web-assets",
  "private": true,
  "description": "Build-time dependencies for the local URDF hand preview",
  "scripts": {
    "build": "esbuild src/hand-preview.js --bundle --format=iife --outfile=static/hand-preview.js --minify --legal-comments=linked"
  },
  "dependencies": {
    "three": "0.185.1",
    "urdf-loader": "0.13.1"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
